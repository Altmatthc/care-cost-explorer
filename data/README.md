Generated data lands here after the first pipeline run.

  <region>-hospitals.json   registry: names, addresses, coordinates, CMS ratings
  <region>.json             prices the website reads

Both are safe to delete; the workflow rebuilds them.
